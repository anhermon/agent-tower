engine-strict=false
auto-install-peers=true
# Fallback store path for Linux environments where ~/.local/share/pnpm is not writable
# (e.g. Docker, CI, or sandboxed agent runners). pnpm uses this only when the default
# global store is unavailable — workspace node_modules/ is always gitignored.
store-dir=node_modules/.pnpm-store
