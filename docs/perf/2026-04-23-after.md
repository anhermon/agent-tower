# Perf After — 2026-04-23

Post-enhancement measurement. Source: `next build` output (gzipped First-Load).

## Delta table

| Route | Before | After | Δ | Budget (200 kB) | Status |
|---|---:|---:|---:|---|---|
| `/sessions/[id]` | 493 kB | **114 kB** | **−379 kB (−77%)** | ✓ | passes |
| `/sessions/overview` | 257 kB | 108 kB | −149 kB (−58%) | ✓ | passes |
| `/skills` | 250 kB | 117 kB | −133 kB (−53%) | ✓ | passes |
| `/sessions/costs` | 249 kB | 107 kB | −142 kB (−57%) | ✓ | passes |
| `/sessions/activity` | 247 kB | 108 kB | −139 kB (−56%) | ✓ | passes |
| `/sessions/tools` | 215 kB | 105 kB | −110 kB (−51%) | ✓ | passes |
| `/sessions/projects` | 123 kB | 124 kB | +1 kB | ✓ | unchanged |
| `/skills/[id]` | 127 kB | 109 kB | −18 kB | ✓ | passes |
| shared baseline | 102 kB | 103 kB | +1 kB | n/a | stable |

All 276 unit tests pass. Typecheck clean across all 9 workspaces.

## What shipped

Stream A (shared chart lazy wrapper):
- `apps/web/components/sessions/charts/_lazy.tsx` — `next/dynamic({ ssr:false })` wrappers for every Recharts-backed chart.
- `apps/web/components/skills/_lazy.tsx` — same pattern for skills-timeline / skills-breakdown-chart / hour-breakdown-chart.
- `apps/web/components/sessions/charts/sparkline.tsx` — rewritten as inline SVG `<polyline>`, no Recharts dep, no `"use client"`.
- Four route files + `skills-dashboard.tsx` retargeted to import from `_lazy`.

Stream B (`/sessions/[id]` specific):
- `apps/web/components/sessions/replay/code-block.tsx` — new leaf, uses `PrismLight` + `registerLanguage` for a 13-language subset (ts/js/tsx/jsx/json/bash/python/go/markdown/yaml/diff/html/css) instead of the full Prism bundle.
- `assistant-markdown.tsx` — `CodeBlock` now `dynamic()`-loaded.
- `turn-card.tsx` — `AssistantMarkdown` now `dynamic()` with `<pre>` fallback.
- `token-accumulation-chart-lazy.tsx` — new client leaf wrapping the chart dynamically; imported from the server `session-detail.tsx`.

Stream C (lazy `DayPicker`):
- `date-range-picker.tsx` — `DayPicker` + its CSS are now loaded only when the popover opens (runtime `import()` inside a `useEffect` gated by a `cssLoadedRef`).

## Not regressed

- Server-side data aggregation (`sessions-analytics.ts`) untouched — was already correct.
- `SkillGridItem` RSC-flight strip intact.
- No new deps. No package.json edits.
- All public component APIs (props, named exports) unchanged.

## Follow-ups (deferred, out of scope today)

- Virtualize turns in `/sessions/[id]` when `turns.length > 100` (runtime only,
  doesn't affect First-Load but reduces TTI on large sessions). Reach for
  `content-visibility: auto` first.
- Memoize `TurnCard` once the above is in place.
- **LHCI:** Full-route results and assert config notes are in `2026-04-23-lhci-full-sweep.md`.
- `size-limit` budgets in `.size-limit.json` are now pessimistic — tighten
  them in a follow-up PR once the LHCI run validates the improvements.
